# test-project

Static demo site used to verify the GitHub + Vercel workflow.

## Local preview
```bash
npx serve public
```

## Deploy to Vercel
```bash
vercel deploy --temporary   # no login, claim later
vercel deploy --prod        # after `vercel login`
```
